{
  "_comment": "SHAPE ONLY -- no real values. Decrypt terraform.tfvars.sops.json instead of copying this file. The angle brackets below are placeholders, not syntax. Generate: authToken = openssl rand -hex 32; everything else = openssl rand -hex 24. The two 'same value as' entries are not a suggestion -- they are the other end of one credential. See README.md for who reads each property.",

  "secret_engines": [
    {
      "name": "homerun2",
      "path": "tabletennis",
      "description": "tabletennis cluster application secrets",
      "data_json": "{\"authToken\":\"<openssl rand -hex 32>\",\"redisPassword\":\"<openssl rand -hex 24>\"}"
    },
    {
      "name": "schmetterpause",
      "path": "tabletennis",
      "description": "tabletennis cluster application secrets",
      "data_json": "{\"session-key\":\"<openssl rand -hex 24>\",\"username\":\"admin\",\"password\":\"<openssl rand -hex 24>\"}"
    },
    {
      "name": "zaehlwerk",
      "path": "tabletennis",
      "description": "tabletennis cluster application secrets",
      "data_json": "{\"omni-pitcher-token\":\"<same value as homerun2 authToken>\",\"redis-password\":\"<same value as homerun2 redisPassword>\"}"
    }
  ],

  "kv_policies": [
    {
      "name": "read-tabletennis",
      "capabilities": "path \"tabletennis/data/homerun2\" { capabilities = [\"read\"] }\npath \"tabletennis/data/schmetterpause\" { capabilities = [\"read\"] }\npath \"tabletennis/data/zaehlwerk\" { capabilities = [\"read\"] }"
    }
  ]
}
